# The subscriber database lives here. Nothing in this folder is ever served.
Require all denied

# Older Apache, in case the host runs 2.2
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
